# Claude connector

**Status: Planned**. This project has no working Claude Web connector, MCP URL or
OAuth flow. No account tier or client version has been validated.

M1.1 and M3.3 must verify the current official client requirements, supported MCP
transport, OAuth discovery/registration, consent and tool behavior. Test explicit
target selection, read/mutation authorization, run polling and interruption before
publishing setup instructions. Compatibility must be documented independently of
the ChatGPT integration.

The example origin `https://gateway.example.com` is illustrative only and must not
be treated as a service. See [MCP tools](mcp-tools.md) and
[authentication](authentication.md).
