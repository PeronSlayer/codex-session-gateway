# Bootstrap status

**Status: Implemented** — this record describes the local scaffold preparation.
Runtime verification and dependency resolution remain **Planned**.

The destination was empty and owned by the unprivileged project user. Node,
Corepack and pnpm remain unavailable. GitHub CLI is installed and authenticated;
the maintainer supplied the Git identity, which is configured locally. No runtime
was installed by the scaffold task and no system configuration was changed.

The source is prepared for publication under `peronslayer/codex-session-gateway`.
Publication does not imply a release, a generated lockfile or successful checks.
The Git author email is commit attribution; a private reporting route still needs
separate confirmation.

## Required completion steps (M0)

1. Make Node.js 24 LTS and pnpm 10.34.5 available through approved provisioning.
   Follow [Node downloads](https://nodejs.org/en/download) and
   [pnpm installation](https://pnpm.io/installation). Do not run remote shell
   pipelines or use privileged npm/pnpm installations for this project.
2. Run `pnpm install --lockfile-only` once to generate the missing `pnpm-lock.yaml`.
   Review the dependency graph and lifecycle build allowlist. This bootstrap
   operation resolves dependencies; it is not a deterministic verification step.
3. Run `pnpm install --frozen-lockfile`, then `pnpm format`, followed by every
   verification command in [development](development.md). Inspect the full diff.
4. Confirm the private reporting routes described in [SECURITY](../SECURITY.md)
   and [Code of Conduct](../CODE_OF_CONDUCT.md).
5. After runtime provisioning, review the generated lockfile and all check results
   before committing the bootstrap completion. Verify hosted CI, CodeQL and
   dependency review separately. Do not add success badges before verification.

## Checks available without Node

Independent source inspection checked JSON/YAML syntax, component metadata,
documentation file links, ignore rules, workflow definitions and the upstream
Apache license text. A preliminary sensitive-file and common-credential pattern
review found no matches. These inspections do not replace the Node-based scripts,
TypeScript compilation, ESLint, Prettier or Vitest; none has run locally.

## Known structural differences

- `vitest.config.ts` uses `test.projects`, replacing the requested deprecated
  `vitest.workspace.ts`; see [Vitest projects](https://vitest.dev/guide/projects).
- Root `tsconfig.json` supplies build references. `tsconfig.check.json` includes
  both tests and sources. `.prettierignore` preserves upstream legal documents.
- `pnpm-lock.yaml` is intentionally absent until generated by the pinned package
  manager. A fabricated or empty lockfile would not support frozen installation.
- All packages are private pending a release policy. No publishing workflow exists.

The configured CI intentionally requires a real lockfile. It will not pass during
this incomplete bootstrap. Source inspection is not a substitute for running the
configured toolchain.
